{% extends 'base.html.twig' %}

{% block title %}Fraisforfait index{% endblock %}

{% block body %}
    <h1>Fraisforfait index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>Montant</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for fraisforfait in fraisforfaits %}
            <tr>
                <td>{{ fraisforfait.id }}</td>
                <td>{{ fraisforfait.libelle }}</td>
                <td>{{ fraisforfait.montant }}</td>
                <td>
                    <a href="{{ path('fraisforfait_show', {'id': fraisforfait.id}) }}">show</a>
                    <a href="{{ path('fraisforfait_edit', {'id': fraisforfait.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('fraisforfait_new') }}">Create new</a>
{% endblock %}
