<form method="post" action="{{ path('fraisforfait_delete', {'id': fraisforfait.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fraisforfait.id) }}">
    <button class="btn">Delete</button>
</form>
