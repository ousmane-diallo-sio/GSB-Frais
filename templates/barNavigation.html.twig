<div class="navBar">
        <div>
                <img rel="icon" src="{{asset('/img/logo.png')}}" style="width: 4em; height: 4em; vertical-align: middle; margin-right: 1em" />
                <a style="color:#fff; font-weight: bold" href={{path('accueilUtilisateur')}}>Accueil</a>
                <a style="color:#fff; font-weight: bold" href={{ path('consulterFicheFrais') }}>Consulter fiches de frais</a>
        </div>
        <div>
                <button style="background-color: var(--shark); border: white dashed 0.1em"><a style="color: white; font-weight: bold" href={{ path('seDeconnecter') }}>Se déconnecter</a></button>
                <p style="color:#fff">{{app.session.get('nom')}} {{app.session.get('prenom')}}</p>
        </div>        
</div>

